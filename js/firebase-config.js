// Fill this in with YOUR Firebase project's config object.
// Get it from: Firebase Console → Project Settings → General → Your apps → SDK setup and config
// This is safe to be public — Firebase web config is not a secret. Access control is handled
// by firestore.rules, not by hiding this object.

export const firebaseConfig = {
  apiKey: "YOUR_API_KEY",
  authDomain: "YOUR_PROJECT.firebaseapp.com",
  projectId: "YOUR_PROJECT",
  storageBucket: "YOUR_PROJECT.appspot.com",
  messagingSenderId: "YOUR_SENDER_ID",
  appId: "YOUR_APP_ID"
};
